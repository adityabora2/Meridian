# Meridian
Adaptive RAG system implementing Adaptive-RAG - routes queries through 3 modes (direct answer, single-hop retrieval, multi-hop retrieval with self-critique) before touching the vector store. Built with LangGraph, Groq, and FAISS.
